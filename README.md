# aboutme

To install dependencies:

```bash
bun install
```

To run:

```bash
bun run dev
```

Open http://localhost:3000

`bun run build` bundles `@material/web` into `bundle.js` (browsers cannot resolve bare package imports on their own).
