{
  "name": "aboutme",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "bun build ./index.ts --outfile=./bundle.js --target=browser",
    "dev": "bun run build && bun --hot server.ts",
    "start": "bun run build && bun server.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@material/web": "^2.5.0"
  }
}
